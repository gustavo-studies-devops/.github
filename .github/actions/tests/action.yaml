name: Application tests
description: Action to running the application tests

runs:
  using: "composite"
  steps:
    - name: Running the tests
      run: |
        cd src/
        npm install
        npm run test
      shell: bash